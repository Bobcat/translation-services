"""The shared per-page regression machinery: the fixture/snapshot model, the deterministic
replay (parse hint -> align -> render), the align/re-OCR comparison, and the reviewer
artifacts. Consumed by the image harness directly and by the pdf harness per page."""
